package chapter_4;

public class Chapter_4 {
	//class 틀, 이 틀에 맞는 알맹이 instance
	//class엔 field와 method 멤버변수와 멤버함수
	//class엔 생성자가 있음 리턴타입이 아예 없는 클래스 이름과 같은 함수
	//default constructor 기본 생성자 매개변수 없음
	//this 클래스가 아니라 인스턴스 자신에 대한 레퍼런스변수
	//method에서 사용됨, static method에선 사용 불가능
	//this() 는 같은 클래스의 다른 생성자를 부를 때 사용
	//constructor의 제일 첫줄에 있어야함
	
	//method는 함수
	//method overloading : 이름이 같은 method가 매개변수가 다를 때
	//받는 매개변수에 따른 method 호출
	
	//privated default protected public
	
	//static 멤버
	//클래스 당 하나만 생성되며 객체들에 의해 공유됨
	//인스턴스가 생성되기 전에 이미 생성됨
	//클래스의 이름으로도, 객체로도 접근이 가능
	//전역 변수와 전역 함수를 만들 때 사용
	//단 전역함수엔 non-static은 사용할 수 없음
	
	//final 마지막 상수
	//상속, 오버라이딩 등등 상태가 변하는 모든 것이 불가능
}
